
<div id="AccountIndex">
    <v-app>
        <div class="container">
            <div class="col s10 m6 l6">
                <h5 class="breadcrumbs-title">{{ title }}</h5>
            </div>
            <v-card>
                <v-card-title>
                    <partial name="_AccountCreationDialog" />
                    <v-btn color="success"
                           slot="activator"
                           href="/Setting/RoleIndexAsync"
                           class="white--text">
                        Quản lý Role
                        <v-icon right dark>verified_user</v-icon>
                    </v-btn>
                    <v-btn color="error"
                           slot="activator"
                           href="/Setting/QuyenIndexAsync"
                           class="white--text">
                        Quản lý Quyền
                        <v-icon right dark>security</v-icon>
                    </v-btn>
                    <v-spacer></v-spacer>
                    <v-text-field v-model="searchUser"
                                  append-icon="search"
                                  label="Tìm Kiếm"
                                  single-line
                                  hide-details></v-text-field>
                </v-card-title>
                <partial name="_AccountDataTable" />
            </v-card>

            <partial name="_AccountEditDialog" />

            <partial name="_Toast" />
        </div>
    </v-app>
</div>
<style>
    .v-input__slot:before, .v-input__slot:after {
        display: none
    }

    .v-small-dialog a {
        text-decoration-style: dashed;
        text-decoration-line: underline;
        text-decoration-color: #0088cc;
    }
</style>

<script src="https://unpkg.com/axios/dist/axios.min.js"></script>
<script src="~/js/accountindex.min.js" type="text/javascript"></script>

